<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mon Stock" Height="400" Width="500">

	<Grid Background="#FFF0F8FF" Margin="20">
		<TextBlock Text="Gestionnaire de Stock Universel"
                   FontSize="24" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,20"/>

		<ListBox Margin="0,80,0,80" Background="White"
                 BorderBrush="LightBlue" BorderThickness="2"
                 FontSize="16"/>

		<Button Content="Rafraîchir" 
                Height="40" Width="120"
                VerticalAlignment="Bottom"
                Background="SkyBlue" Foreground="White"
                FontSize="16"/>

		<TextBlock Text="Interface par [TON PRÉNOM]" 
                   HorizontalAlignment="Right" 
                   VerticalAlignment="Bottom" 
                   FontSize="12" Margin="10"/>
	</Grid>
</Window>